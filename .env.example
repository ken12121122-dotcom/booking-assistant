LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=
GEMINI_API_KEY=
GEMINI_MODEL=

# Vercel KV (Upstash Redis) - optional, enables multi-turn clarifying questions.
# Without these the parser falls back to single-turn (no memory between messages).
KV_REST_API_URL=
KV_REST_API_TOKEN=
